name: Check Suite 5

on:
  pull_request:

jobs:
  check-suite-5:
    runs-on: ubuntu-latest
    steps:
      - name: Echo message
        run: echo "Running Check Suite 5"

      - name: Sleep for 8 seconds
        run: sleep 8

      - name: Continue with next steps
        run: echo "Check Suite 5 completed successfully"
